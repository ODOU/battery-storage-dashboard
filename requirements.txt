# The OnSSET_SENEGAL conda environment already provides all of these.
# runtime of the build and the tests
pandas>=2.0
pytest>=7
# extraction from the manuscript DOCX (scripts/extract_docx.py)
python-docx>=1.1
lxml>=4.9
# alignment with the published PDF (scripts/apply_published_annex.py)
pymupdf>=1.24
# one-off map data (scripts/make_geojson.py)
geopandas>=0.14
